version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use 20
        - npx pnpm install
    build:
      commands:
        - npx pnpm run build
  artifacts:
    baseDirectory: .amplify-hosting
    files:
      - '**/*'
